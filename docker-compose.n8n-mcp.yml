version: '3'

services:
  n8n-mcp:
    build:
      context: ./n8n-mcp
    container_name: n8n-mcp
    environment:
      - VIRTUAL_HOST=n8n-mcp.arisegulf.com
      - VIRTUAL_PORT=3000
      - LETSENCRYPT_HOST=n8n-mcp.arisegulf.com
      - LETSENCRYPT_EMAIL=arisegulf@gmail.com
    networks:
      - app-network

networks:
  app-network:
    external: true
